/* eslint-disable class-methods-use-this */
/* eslint-disable no-console */

// const { query, begin, end, ... } = require('../../lib/database');

class TemplateRepository {
  // DB에 query를 보내는 것은 Repository에서만 가능.
}

module.exports = TemplateRepository;
