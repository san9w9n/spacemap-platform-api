class TrajectoryHandler {
  static async trajectoryParser(trajcetory) {
    return epochDate;
  }

  static async diffSeconds(epochDate, startMomentOfPredictionWindow) {
    return diffSeconds;
  }

  static async isValidDate(epochDate) {}

  static async addSecondsToTrajectory() {}
}

module.exports = TrajectoryHandler;
